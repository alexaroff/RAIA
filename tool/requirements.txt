# См. pyproject.toml — основной источник правды.
# Установка:
#   cd tool && pip install -e ".[vad]"
#   # опционально: pip install -e ".[vad,gigaam]"

mlx>=0.22.0
mlx-whisper>=0.4.3
soundfile>=0.12.1
numpy>=1.26
rich>=13.0
python-dotenv>=1.0
psutil>=5.9
